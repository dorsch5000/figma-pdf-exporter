{
  "name": "pdf-exporter-plugin",
  "version": "1.0.0",
  "description": "Figma plugin to export selected frames as a multi-page PDF with configurable image compression",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "^1.123.0",
    "typescript": "^5.9.3"
  }
}
